import { localeDe } from './locales_de';
import { localeAr } from './locales_ar';
import { localeFr } from './locales_fr';

export const LOCALES: Record<string, any> = {
  DE: localeDe,
  AR: localeAr,
  FR: localeFr
};
